// -------------------------------------------------------
// No Prisma schema needed for this project.
// This calculator is entirely client-side with no
// database, authentication, or persistent storage.
//
// If you later add features like:
//   - Calculation history
//   - User preferences (theme, decimal places)
//   - Saved expressions
// ...you can introduce a schema at that point.
// -------------------------------------------------------

// Potential future schema (commented out as reference):
//
// generator client {
//   provider = "prisma-client-js"
// }
//
// datasource db {
//   provider = "sqlite" // lightweight for personal use
//   url      = env("DATABASE_URL")
// }
//
// model CalculationHistory {
//   id         String   @id @default(cuid())
//   expression String
//   result     String
//   createdAt  DateTime @default(now())
// }
//
// model UserPreference {
//   id            String  @id @default(cuid())
//   theme         String  @default("dark") // 'dark' | 'light'
//   decimalPlaces Int     @default(10)
//   soundEnabled  Boolean @default(false)
//   createdAt     DateTime @default(now())
//   updatedAt     DateTime @updatedAt
// }